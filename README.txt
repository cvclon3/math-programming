Мат прога

Формат работы
-работаем с целой матрицей
-создаем транспортный класс для матрицы

class Data:
	A_mtx_
	...

class Solver:
	def __init__(self, data_: Data):
		self.Solution: np.array[Transport] = np.array([])
		self.Solution = np.append(self.Solution, prepare_data(data: Data))
		pass

	def solve(None) -> np.array[Transport]:
		pass

	цикл
		ARR = Transport()
	END

class Transport:
	def __init__(self, table: np.ndarray):
		table_: np.ndarray() = np.array()

def calc(obj: Transport, *args, **kwargs) -> Transport:
	pass

-ооп + разбиение на функции
-принцип единственности
-ПИШЕМ КОММЕНТЫ!!!!

Что надо сделать
	
def prepare_data(data: Data) -> Transport:
	pass

def calc(obj: Transport, *args, **kwargs) -> Transport:
	pass

	

